
<h1>Welcome to <i>Dérive</i>! </h1>
<h3>Hello <%= @user.name %>!</h3>

<p>Your Interests:</p>
<% if @user.interests.empty? %>
<p><b>Select some interests!</b></p>
<% else %>
  <ul>
    <% @user.interests.each do |i| %>
    <li><%= i.name %></li>
    <% end %>
  </ul>
<% end %>
<%= form
 <hr>

<p>Selected Destinations:</p>
<% if @user.destinations.empty? %>
<p><b>Select some destinations!</b></p>
<% else %>
  <ul>
    <% @user.destinations.each do |d| %>
    <li><%= link_to "#{d.city} in #{d.country}", destination_path(d) %></li>
    <% end %>
  </ul>
<% end %> <hr>

<h3>Destinations Recommendations:</h3>
<ul>
<% @destinations.each do |d| %>
<li><%= link_to "#{d.city} in #{d.country}", destination_path(d)%></li>
<% end %> </ul>
